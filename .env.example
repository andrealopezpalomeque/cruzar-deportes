# Shared environment configuration for Cruzar Deportes
# Copy this file to .env and fill in the values before running any sync/deploy scripts.

# Absolute path to the Firebase service account JSON with Storage access
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/cruzar-back-office-service-account.json

# Firebase project that hosts the shared product catalog
GCLOUD_PROJECT=cruzar-back-office

# Cloud Storage bucket where shared/products.json is stored
FIREBASE_STORAGE_BUCKET=cruzar-back-office.appspot.com

# Optional: explicit Firebase config blob (only needed in some hosting setups)
# FIREBASE_CONFIG={"projectId":"cruzar-back-office","storageBucket":"cruzar-back-office.appspot.com"}
