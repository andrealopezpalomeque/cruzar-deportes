{
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "sync:data": "cd apps/back-office && node scripts/bootstrap-storage.ts && node scripts/rebuild-catalog.ts",
    "deploy": "firebase deploy",
    "deploy:storefront": "firebase deploy --only hosting:storefront",
    "deploy:admin": "firebase deploy --only hosting:admin,functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.4.0"
  }
}
